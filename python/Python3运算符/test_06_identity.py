# ============================================
# 题目6：身份运算符练习
# ============================================
# 完成以下操作：
# 1. 创建 a = [1, 2, 3]，b = a，c = a[:]
# 2. 测试 a is b、a is c、a == b、a == c
# 3. 用 id() 查看内存地址
# ============================================

# 在下面写你的代码：
a = [1, 2, 3]
b = a
c = a[:]
print(a is b)
print(a is c)
print(a == b)
print(a == c)

print(id(a))
print(id(b))
print(id(c))


# ============================================
# 参考答案（写完后取消注释对比）
# ============================================
# a = [1, 2, 3]
# b = a
# c = a[:]
#
# print(f"a is b: {a is b}")     # True（同一个对象）
# print(f"a is c: {a is c}")     # False（不同对象）
# print(f"a == b: {a == b}")     # True（值相等）
# print(f"a == c: {a == c}")     # True（值相等）
#
# print(f"id(a): {id(a)}")
# print(f"id(b): {id(b)}")  # 和 a 相同
# print(f"id(c): {id(c)}")  # 和 a 不同
