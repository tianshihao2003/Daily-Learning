# ============================================
# 题目1：创建元组
# ============================================
# 完成以下操作：
# 1. 创建元组 t1 = (1, 2, 3, 4, 5)
# -> (元素1, 元素2, ...)
# 2. 创建元组 t2 = ("a", "b", "c")
# -> (元素1, 元素2, ...)
# 3. 创建只有一个元素的元组 t3 = (10,)（注意逗号）
# -> (元素,) 单元素元组要加逗号
# 4. 打印每个元组及其类型
# -> type(对象)
# ============================================

# 在下面写你的代码：
t1 = (1, 2, 3, 4, 5)
t2 = ("a", "b", "c")
t3 = (10,)

print(type(t1), type(t2), type(t3))


# ============================================
# 参考答案（写完后取消注释对比）
# ============================================
# t1 = (1, 2, 3, 4, 5)
# t2 = ("a", "b", "c")
# t3 = (10,)
#
# print(f"t1: {t1}, 类型: {type(t1)}")
# print(f"t2: {t2}, 类型: {type(t2)}")
# print(f"t3: {t3}, 类型: {type(t3)}")
