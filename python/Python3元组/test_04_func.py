# ============================================
# 题目4：元组常用函数
# ============================================
# 完成以下操作：
# 1. 创建元组 t = (3, 1, 4, 1, 5, 9, 2, 6)
# -> (元素1, 元素2, ...)
# 2. 用 len() 计算长度
# -> len(tuple)
# 3. 用 max() 获取最大值
# -> max(tuple)
# 4. 用 min() 获取最小值
# -> min(tuple)
# ============================================

# 在下面写你的代码：
t = (3, 1, 4, 1, 5, 9, 2, 6)

print(len(t))
print(max(t))
print(min(t))


# ============================================
# 参考答案（写完后取消注释对比）
# ============================================
# t = (3, 1, 4, 1, 5, 9, 2, 6)
#
# print(f"长度: {len(t)}")    # 8
# print(f"最大值: {max(t)}")  # 9
# print(f"最小值: {min(t)}")  # 1
