# ============================================
# 题目6：元组不可变性验证
# ============================================
# 完成以下操作：
# 1. 创建元组 t = (1, 2, 3)
# -> (元素1, 元素2, ...)
# 2. 尝试修改 t[0] = 10，观察报错信息
# -> tuple[索引] = 值（会报错）
# 3. 用 id() 查看修改前后元组的内存地址
# -> id(对象)
# ============================================

# 在下面写你的代码：
t = (1, 2, 3)

# t[0]=10
print(id(t))

t = (10, 2, 3)
print(id(t))


# ============================================
# 参考答案（写完后取消注释对比）
# ============================================
# t = (1, 2, 3)
# print(f"修改前: {t}")
# print(f"修改前地址: {id(t)}")
#
# # 尝试修改会报错：TypeError: 'tuple' object does not support item assignment
# # t[0] = 10
#
# t = (10, 2, 3)  # 重新赋值
# print(f"重新赋值后: {t}")
# print(f"重新赋值后地址: {id(t)}")  # 地址不同，是新对象
