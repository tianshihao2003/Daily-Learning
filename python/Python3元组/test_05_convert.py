# ============================================
# 题目5：元组与列表转换
# ============================================
# 完成以下操作：
# 1. 创建列表 lst = [1, 2, 3, 4, 5]
# -> [元素1, 元素2, ...]
# 2. 用 tuple() 将列表转换为元组
# -> tuple(列表)
# 3. 创建元组 t = (6, 7, 8)
# -> (元素1, 元素2, ...)
# 4. 用 list() 将元组转换为列表
# -> list(元组)
# ============================================

# 在下面写你的代码：
lst = [1, 2, 3, 4, 5]

lst = tuple(lst)
print(lst)

t = (6, 7, 8)

t = list(t)
print(t)


# ============================================
# 参考答案（写完后取消注释对比）
# ============================================
# lst = [1, 2, 3, 4, 5]
# t = tuple(lst)
# print(f"列表转元组: {t}")  # (1, 2, 3, 4, 5)
#
# t2 = (6, 7, 8)
# lst2 = list(t2)
# print(f"元组转列表: {lst2}")  # [6, 7, 8]
