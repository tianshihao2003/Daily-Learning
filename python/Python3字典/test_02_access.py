# ============================================
# 题目2：访问字典元素
# ============================================
# 完成以下操作：
# 1. 创建字典 d = {"name": "李四", "age": 25, "job": "工程师"}
# -> {key: value, key: value}
# 2. 用 [] 访问 name 的值
# -> dict[key]
# 3. 用 get() 访问 age 的值
# -> dict.get(key)
# 4. 用 get() 访问不存在的键 salary，设置默认值 0
# -> dict.get(key, 默认值)
# ============================================

# 在下面写你的代码：
d = {"name": "李四", "age": 25, "job": "工程师"}

print(d["name"])
print(d.get("age"))
print(d.get("aaa", 0))


# ============================================
# 参考答案（写完后取消注释对比）
# ============================================
# d = {"name": "李四", "age": 25, "job": "工程师"}
#
# print(f"name: {d['name']}")        # 李四
# print(f"age: {d.get('age')}")      # 25
# print(f"salary: {d.get('salary', 0)}")  # 0（不存在返回默认值）
