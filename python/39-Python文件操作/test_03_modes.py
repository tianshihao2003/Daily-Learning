# ============================================
# 题目3：with 与三种模式
# ============================================
# 完成以下操作：
# 1. 用 with open(...) 重写写文件（不用手动 close）
# 2. 先用 "w" 写入两行内容，再用 "a" 追加一行，最后读出来打印
# 3. 回答问题：如果第二步也用 "w"，结果会有什么不同？
# 4. 再试一次 "r" 模式写入，看看会发生什么（会报错，记住错误信息）
# ============================================

# 在下面写你的代码：


