# ============================================
# 题目1：用字符串方式创建第一个 Agent
# ============================================
# 完成以下操作：
# 用 create_agent("deepseek-v4-flash") 创建 Agent，打印 type(agent)
# invoke 一句"你好"，取出最后一条消息的 content 并打印
# 先把 load_dotenv(override=True) 写上，否则密钥读不到
# ============================================

# 在下面写你的代码：
