# ============================================
# 题目 2-2：同一个仓库、四种下刀方式——切分器家族都在哪儿用
# ============================================
# 完成以下操作：
# 先造一段 200 字以上、同时含 Markdown 标题（# 与 ##）和中文句子的文本，然后：
# 1. 用最常用的那种切分器切开（块上限 80 字、重叠 20 字），打印每块的字符数与内容
# 2. 让它顺便把每块的起始位置记进元数据，把元数据打印出来
# 3. 换成"Markdown 语法版、块大小可控"的那种切分器切同一段（记得打开它的正则分隔符开关），
#    比较和上一步的块有什么不同
# 4. 用"按标题切并保留标题层级"的那种切分器再切一次（把标题映射成中文键名），
#    打印每块的正文与元数据，说明标题去哪了
# 5. 最后用"按 token 数切"的那种切分器切一段中文，检查有没有乱码，并解释原因和结论
# ============================================

# 在下面写你的代码：
