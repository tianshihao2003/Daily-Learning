# ============================================
# 题目 2-1：三种数据源、一个结果——把不同来源都变成同一种对象
# ============================================
# 完成以下操作：
# 1. 自己造一个本地文本文件并加载它，打印文档数、正文、元数据
# 2. 抓一个网页（换成你能访问的任意链接），打印它的元数据（注意比本地文件多出哪些字段）
#    以及正文前 80 个字符
# 3. 往一个子文件夹里放 3 个小文件，用"批量加载"把整个文件夹加载进来：
#    只挑其中一种后缀，并指定用"加载文本文件的那个加载器"作为底层实现，打印加载条数
# 4. 依赖体检：用 PDF 加载器加载一份 PDF（可以用 Python 现场生成一份），看它报什么错、
#    该按提示补哪个包；再换成本机可用的 PDF 替代加载器把同一份 PDF 加载成功，
#    打印每页的页码与总页数；接着随手试一个 Word / Markdown / HTML 里的加载器，看它缺什么包
# 5. 最后回答一句：为什么这四种来源的输出可以用同一种方法读？
# ============================================

# 在下面写你的代码：
