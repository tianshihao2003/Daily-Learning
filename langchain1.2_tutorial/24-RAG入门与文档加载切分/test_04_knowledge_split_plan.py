# ============================================
# 综合题：给客服知识库设计一套"加载 → 切分 → 自检"流水线
# ============================================
# 完成以下操作：
# 自造一份知识库文件夹，里面放一份文本文件，内容为 3 条以上、每条 80 字以上的客服问答，
# 条目之间用 \n==============================\n 分隔。然后：
# 1. 用"批量加载"把整个文件夹加载进来（只挑 .txt、底层用加载文本文件的加载器）
# 2. 用递归切分器切：业务分隔线排第一优先、块上限 120 字、不重叠、显式关掉分隔符保留，
#    并让每块元数据带上起始位置
# 3. 打印自检表：每块字符数、来源、起始位置、内容前 50 字；并专门检查有没有
#    "只装分隔线的垃圾块"
# 4. 换"语义分块"再切同一份文本（本机没有嵌入服务，用那条不联网的确定性伪嵌入顶替，
#    断点阈值用"百分位"那一种、取 65.0，句子正则按中文写），比较两种切法的块数，
#    并回答：这个语义分块结果能不能当参考？
# 5. 一句话结论：这份中文知识库该用哪个切分器；如果换成 500 份扫描版 PDF，
#    "加载"这一步该让位给谁、它比普通 PDF 加载器多出哪些能力（举两样）、
#    向它提交任务时的请求体关键字段有哪些（哪些开关控制 OCR / 公式 / 表格）
#    ——这一步只写代码不运行
# ============================================

# 在下面写你的代码：
