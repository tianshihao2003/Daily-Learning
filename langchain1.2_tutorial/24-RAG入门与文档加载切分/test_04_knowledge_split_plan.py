# ============================================
# 综合题：给一份知识文档设计切分方案
# ============================================
# 完成以下操作：
# 自造一份"客服知识库"文本：3 条较长的问答（每条 100 字以上），条目之间用 \n==============================\n 分隔
# 用 TextLoader 加载成 Document
# 用 RecursiveCharacterTextSplitter(chunk_size=100, chunk_overlap=0, separators=[分隔线, "\n\n", "\n", "。", " "]) 切分，打印每块的字符数和内容
# 观察两个现象：① 长条目会被切成两块 ② 那行分隔线自己成了一个块；再加 keep_separator=False 再切一次，看分隔线块是否消失
# 再用 TokenTextSplitter(chunk_size=30) 切同一段中文，检查有没有 � 乱码
# ============================================

# 在下面写你的代码：
