# ============================================
# 题目 2-3：分隔符与重叠的脾气——把切块"调"成想要的样子
# ============================================
# 完成以下操作（素材：这是第一段文本。这是第二段内容。最后一段结束。）：
# 1. 以中文句号为分隔符、块上限 20 字、重叠 8 字，分别用"丢弃分隔符（默认）"、
#    "分隔符留在下一块开头"、"分隔符留在上一块结尾"三种设置各切一遍，
#    把三组结果打印出来，说明句号去哪了
# 2. 把块上限压到比单个句子还小（比如 6 字），观察控制台警告与每块的实际长度，
#    回答：块上限是"目标"还是"硬约束"？此时重叠还有没有生效？
# 3. 造一份 2~3 条、用 \n==============================\n 分隔的知识库文本，
#    按"分隔线优先"的递归切分（块上限 100、不重叠）分别在"保留分隔符"和"不留分隔符"
#    两种设置下切一遍，打印块数与每块内容，解释为什么会出现一个只装等号的分隔线块，
#    以及该怎么消掉它
# ============================================

# 在下面写你的代码：
