# ============================================
# 题目 2-2：让 Agent 跨会话记住你
# ============================================
# 需求：
# 1. 写两个工具：一个把用户信息写进长期记忆，一个从长期记忆读回来
#    两个工具都要通过"框架自动注入的那个运行时参数"拿到记忆仓库和当前用户 ID
# 2. 用户 ID 不要写死：用自定义状态字段带进来，调用时一起传
# 3. 用会话 A 说"我叫韩立"，然后把仓库里存下的内容打印出来
# 4. 换一个全新的会话 ID 问"我叫什么？"，确认它照样答得上来
# 5. 回答：为什么换了会话（甚至换一次进程）它还记得？
# ============================================

# 在下面写你的代码：
