# ============================================
# 题目 3-1（综合）：一个"额度守卫"中间件——鉴权 + 计费 + 写回
# ============================================
# 场景：每个用户的"剩余额度"和"能用哪些工具"都存在长期记忆里。
# 需求：
# 1. 准备长期记忆数据：三个用户——额度充足 / 没有天气工具权限 / 额度太少
# 2. 写一个中间件类，同时实现三道关卡：
#    ① 模型调用前：按本次调用传入的用户身份，从长期记忆读出该用户能用的工具，
#       把不能用的从"这次"请求的工具集里去掉（只对本次生效，别污染下一轮），
#       并为每个被去掉的工具打印一行日志
#    ② 工具调用前：算出本次调用要花多少额度（自己定一张价目表）
#       够 → 扣掉并写回长期记忆（同一用户的其他字段要保住），然后放行真工具
#       不够 → 不执行真工具，直接返回一条"额度不足"的工具结果
#              （注意把工具调用的编号回填进去，否则模型那边等不到回音）
#    ③ Agent 跑完之后：从长期记忆读出最新额度打印出来
# 3. 三个用户各跑一次（都问"帮我查一下北京今天的天气"），
#    把日志和仓库里的剩余额度对照着看
# 4. 回答：为什么被拦下的那次额度一分没扣？
# ============================================

# 在下面写你的代码：
