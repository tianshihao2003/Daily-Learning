# ============================================
# 题目 2-3：按用户偏好定制系统提示
# ============================================
# 需求：
# 1. 先把两条"聊天偏好"写进长期记忆（两个不同用户、不同偏好，比如"不喜欢啰嗦"/"喜欢长篇大论"）
# 2. 定义一个"用户上下文"结构（只有一个用户名字段）
# 3. 写一个便捷钩子：按本次调用传进来的用户身份，从长期记忆里取出该用户的偏好，
#    拼成这次要用的系统提示词，并打印出来
# 4. 用同一个 Agent、两个不同身份各跑一次同一个问题（"为什么花儿这样红？"）
# 5. 打印返回结果里的消息类型列表，回答：这条动态生成的系统提示词
#    有没有出现在消息列表里？它凭什么能每次都重新算？
# ============================================

# 在下面写你的代码：
