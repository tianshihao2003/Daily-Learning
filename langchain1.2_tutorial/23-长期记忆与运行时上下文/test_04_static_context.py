# ============================================
# 题目 2-4：静态上下文——给同一个 Agent 换身份
# ============================================
# 需求：
# 1. 定义一个"用户上下文"结构（只有一个用户 ID 字段），三步接上：
#    定义它 → 创建 Agent 时声明它 → 调用时传具体值
# 2. 写两个工具：一个把用户资料写进长期记忆、一个读回来
#    用户 ID 要从静态上下文里取，而不是从状态里取
#    工具参数用一个数据模型写（用户名 / 年龄 / 爱好）
# 3. 加一个"模型调用前"的钩子，打印本次传进来的用户 ID
# 4. 用两个不同的身份跑同一个 Agent，确认两次写入落在不同的槽位里
#    （第二个身份问"我之前告诉过你什么？"，应该查不到）
# 5. 顺带看一眼这个工具对外的 JSON Schema：那个数据模型的参数长什么样、
#    框架自动注入的那个参数在不在里面
# 6. 回答：静态上下文、状态、长期记忆三者的分工是什么？
# ============================================

# 在下面写你的代码：
