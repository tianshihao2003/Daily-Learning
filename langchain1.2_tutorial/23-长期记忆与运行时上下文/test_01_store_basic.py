# ============================================
# 题目1：直接操作 store
# ============================================
# 完成以下操作：
# 用 InMemoryStore() 存两条记忆（不同 key，同一个 ("users", "alice", "memories") 命名空间）
# ① 用 get() 取其中一条，打印 item.value（注意是 Item 对象，要 .value）
# ② 用 search(("users",)) 按前缀把两条都查出来
# ③ 用 search(ns, filter={...}) 只筛出其中一条
# ============================================

# 在下面写你的代码：
