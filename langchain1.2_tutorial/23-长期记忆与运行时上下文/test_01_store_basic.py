# ============================================
# 题目 2-1：直接操作记忆仓库
# ============================================
# 需求：
# 1. 用内存版仓库，在同一个命名空间（("users", "alice", "memories")）下存两条不同键的记忆
# 2. ① 取出其中一条，打印真正的值（注意取回来的不是裸字典）
#    ② 按前缀把两条都查出来
#    ③ 按 value 里的某个键做精确过滤，只筛出其中一条
# 3. 给仓库配一个"自定义的嵌入函数 + 指定维度"，让 value 的"整体"和"某个一级字段"都建索引
#    存一条数据后把仓库内部的向量结构打印出来，数一数一条数据生成了几个向量
# 4. 用同一个键改写两次（中间隔 1 秒），对比两次读回的时间戳。
#    回答：在内存版仓库里，这次"更新"到底是更新还是新建？
# （本条只操作仓库、不用模型，可以离线跑）
# ============================================

# 在下面写你的代码：
