# ============================================
# 综合题：给客服助手装上"跨会话记忆"
# ============================================
# 完成以下操作：
# 自定义 CustomState 加 user_id 字段；用 store 保存长期记忆
# 写 save_order_no(order_no, runtime) 工具：把订单号写进 store 的 ("orders", user_id)
# 写 get_order_no(runtime) 工具：从 store 读回来
# 系统提示词里要求"用户告知订单号时务必调用工具记录，被问到时先查询再回答"
# 新会话（换 thread_id）问"我的订单号是多少？"，确认它依然能答上来
# ============================================

# 在下面写你的代码：
