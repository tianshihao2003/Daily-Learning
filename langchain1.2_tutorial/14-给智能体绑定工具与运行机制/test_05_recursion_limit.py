# ============================================
# 题目 3-1：给 Agent 加一根"安全绳"：判断完成 + 限制步数
# ============================================
# 需求（分三步）：
# 1. 写一个**永远查不完**的工具：每次调用都返回"数据还没准备好，请再查一次"
#    （用固定前缀标记）；配上系统提示词，要求模型看到这个前缀就再调用一次同一个工具
# 2. 先用"判断完成"的标准写法遍历消息：某条 AI 消息里还有工具调用，
#    就打印"还在调用工具…"；否则打印"完成！最终答案：…"
# 3. 给这次调用加上"最多 5 步"的限制再跑一次，观察：
#    - 超限时抛的是什么异常？（用 try/except 接住并打印异常名和错误信息）
#    - 这一次工具被执行了几次？
#    （提示：一次模型调用算一步、一次工具执行也算一步）
# 4. 想一想：这个限制适合当"防死循环的兜底"，还是"限制次数后继续正常工作"？
#    想"限次数但不报错"该用什么？（见第 20 篇）
# ============================================

# 在下面写你的代码：
