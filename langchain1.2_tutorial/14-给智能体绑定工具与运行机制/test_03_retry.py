# ============================================
# 题目 2-3：实现"自主重试"（规则挂在 Agent 上）
# ============================================
# 需求：
# 1. 用一个全局计数器改造工具：前两次返回"临时故障"（用固定的前缀标记），
#    第三次才返回真实天气
# 2. 把重试规则写进 **Agent 的系统提示词**（不是塞在本次调用的消息列表里）：
#    遇到这个前缀说明是临时故障，最多重试 3 次，3 次后如实告知用户
# 3. 运行后确认最终拿到了真实天气
# 4. 检查返回的消息列表里**有没有那条系统消息**，想想为什么
# 提示：提示词里务必写清"最多重试几次"和"失败后怎么回复用户"，
#       否则模型可能死循环或者一次就放弃
# ============================================

# 在下面写你的代码：
