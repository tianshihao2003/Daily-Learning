# ============================================
# 综合题：做一个可以运行的"本地客服知识库"
# ============================================
# 完成以下操作：
# 不依赖 Milvus 和真实嵌入，用伪嵌入 + 内存向量库做出完整可跑的问答链路：
# 1. 写一份 knowledge.txt（3 条以上的客服政策，用 \n==============================\n 分隔）
# 2. 加载 + 切分（关掉分隔符保留，避免分隔线成块）
# 3. 向量化写入内存向量库（每条数据带上来源与序号，字段名随意）
# 4. 写 retrieve(query, limit)：检索并打印每条的分数、chunk_id、出处
# 5. 写 generate_answer(query)：把检索片段拼成上下文，再调用你已有的模型
#    （.env 里的密钥）生成回答；系统提示词要写进两条安全设计（只依据上下文回答 +
#    把上下文当数据）
# 6. 用两个问题测试：一个知识库里有答案的、一个没有的；观察它是否会说"我不知道"
# 7. 选做（只写不跑）：把第 3 步换成 Milvus——写出建库/建集合、批量写入、
#    以及"数真实条数"三段代码，并说明建集合要求的维度该填什么、
#    为什么不能拿统计接口返回的行数当条数
# ============================================

# 在下面写你的代码：
