# ============================================
# 综合题：做一个可以运行的"本地客服知识库"
# ============================================
# 完成以下操作：
# 不依赖 Milvus 和真实嵌入，用伪嵌入 + 内存向量库做出完整可跑的问答链路：
# 1. 写一份 knowledge.txt（3 条以上客服政策，用 \n==============================\n 分隔）
# 2. 加载 + 切分（keep_separator=False，避免分隔线成块）
# 3. 向量化写入 InMemoryVectorStore（metadata 里放 source 和 chunk_id）
# 4. 写 retrieve(query, limit)：检索并打印每条的分数、chunk_id、原文
# 5. 写 generate_answer(query)：把检索片段拼成上下文，调用模型生成回答
# 6. 用两个问题测试：一个知识库里有答案的、一个没有的；观察它是否会说"我不知道"
# ============================================

# 在下面写你的代码：
