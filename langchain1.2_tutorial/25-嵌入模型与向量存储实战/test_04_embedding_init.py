# ============================================
# 题目 2-4：两种初始化写法 + 维度体检（缺密钥要能优雅降级）
# ============================================
# 完成以下操作：
# 假设你有一个 OpenAI 兼容的嵌入服务（如某厂商的 bge-m3，1024 维），
# 密钥与地址都从环境变量读：
# 1. 用"通用工厂"方式初始化（靠模型名前缀选协议）写一个函数
# 2. 用"直连 OpenAI 兼容接口"的类再写一个函数，并显式声明输出维度
# 3. 写一个入口函数：环境变量齐全就用真模型，缺任一变量就降级到那条不联网的伪嵌入
#    （保证脚本永远能跑），并打印提示
# 4. 维度体检：用拿到的模型向量化一句话，打印实际维度，和"建表要求维度"比对，
#    不一致就打印警告并解释后果
# ============================================

# 在下面写你的代码：
