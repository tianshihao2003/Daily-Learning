# ============================================
# 题目 2-4：加法助手，看模型自己拆解任务
# ============================================
# 需求：
# 1. 写一个只会把两个数相加的工具，返回一句"和为：xx"
# 2. 创建一个数学助手 Agent：系统提示词用"消息对象"的形式传
#    （例如"你是一个数学助手，解决日常的算术问题"）
# 3. 问："10加上20再加上30是多少？"
# 4. 然后检查四件事：
#    ① 模型一共发起了几次工具调用
#    ② 每次调用请求的正文内容是什么（是空的吗？）
#    ③ 两次请求的调用编号与两条结果的配对编号，是否各自对上
#    ④ 返回的消息列表第一条是什么类型（用系统提示词传进去的那条消息去哪了？）
# 提示：工具只会算两个数，"怎么拆成两步"完全是模型自己决定的
# ============================================

# 在下面写你的代码：
