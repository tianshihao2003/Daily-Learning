# ============================================
# 题目 2-2：同一张图，两种多模态写法
# ============================================
# 完成以下操作：
# 1. 写两个小函数：
#    ① 把本地图片读成"带 data:image/...;base64, 前缀的完整字符串"
#    ② 只做 Base64 编码（不带前缀）
# 2. 用同一张 PNG 分别构造两条带图提问的用户消息：
#    ① 供应商规范的字典列表（图片块的值用带前缀的完整字符串）
#    ② 统一的标准块（值是纯 Base64，格式由另一个字段声明）
# 3. 打印两条消息的内容结构做对比
# 4. 说明为什么这两种值不能混着用
# 提示：图片是 PNG 就别用默认的 jpeg 前缀
# ============================================

# 在下面写你的代码：


