# ============================================
# 题目4：运行时覆盖模型参数
# ============================================
# 1. 初始化模型时：用较低的温度、较小的 token 上限，
#    并声明「哪些参数允许在运行时替换」
# 2. 调用时用运行时配置：
#    - 把模型换成更强的那一档
#    - 把温度调高、token 上限调大
#    - 顺便给这次运行起名 / 打标签 / 带上用户 ID 和会话 ID
# 3. 从返回值的元数据里确认「实际生效的是哪个模型」
# 4. 在注释里说明：如果初始化时没做那步声明，会发生什么
# ============================================

# 在下面写你的代码：


