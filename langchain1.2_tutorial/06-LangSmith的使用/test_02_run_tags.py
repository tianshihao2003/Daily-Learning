# ============================================
# 题目2：给 Trace 打标（让记录「能找得到」）
# ============================================
# 在同一次调用上做三件事：
# 1. 给这次运行起一个可读的名字（默认只会显示方法名，看不出业务含义）
# 2. 打上两个便于分类的标签
# 3. 带上业务元数据：用户 ID + 会话 ID
# 调用一句「1 + 2 = ？」，然后在注释里写清楚：
#   - 在官网 Tracing 列表里怎么按名称找到它
#   - 怎么按标签筛选
#   - 在哪里能看到元数据
# ============================================

# 在下面写你的代码：


