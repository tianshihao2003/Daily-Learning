# ============================================
# 题目2：用类写法实现"计数中间件"
# ============================================
# 完成以下操作：
# 写一个继承 AgentMiddleware 的类，在 after_model 里把 state["count"] 自增并存回去（return {"count": ...}）
# 跑多轮对话，观察计数是否在累加（从返回的 result.get("count") 里读）
# 类名可以随意，但方法名必须叫 after_model
# ============================================

# 在下面写你的代码：
