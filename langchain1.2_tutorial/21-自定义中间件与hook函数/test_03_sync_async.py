# ============================================
# 题目 2-3：一个类同时覆盖"同步 + 异步"两条路径
# ============================================
# 需求：
# 1. 写一个中间件类，同时实现同步和异步两套钩子
#    （至少覆盖"模型调用前"和"包裹模型调用"这两对）
#    两套钩子打印不同前缀的标记（比如"同步路径 / 异步路径"）
# 2. 先同步调用一次 Agent，再异步调用一次，确认各自走的是对应那套钩子
# 3. 列出中间件基类上六个 hook 的异步版本名，确认它们齐全
# ============================================

# 在下面写你的代码：
