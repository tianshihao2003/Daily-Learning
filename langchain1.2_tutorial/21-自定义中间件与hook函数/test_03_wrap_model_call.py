# ============================================
# 题目3：用 wrap_model_call 统一追加提示词
# ============================================
# 完成以下操作：
# 用 @wrap_model_call 写一个钩子：在 request.messages[-1].content 后面追加"（请用一句话回答）"
# 把修改后的 request 交给 handler(request)，再 return 响应
# 问一个开放问题（如"介绍一下 Python"），看回答是否变短
# 挑战：在 handler 返回后改 response.result[0].content，观察输出被追加了内容
# ============================================

# 在下面写你的代码：
