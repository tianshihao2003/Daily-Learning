# ============================================
# 题目 3-1（综合）：三种流程跳转——跳过模型、跳回重生成、提前熔断
# ============================================
# 需求：
# 1. 准备一个"查新闻"的工具（返回固定的一条新闻），建一个 Agent
# 2. 用"流程跳转"实现三种行为（注意：每个钩子都要声明自己允许跳到哪）：
#    ① 直跳工具：输入里带 direct tool 时，跳过模型的首轮思考，
#       自己伪造一条"模型要调工具"的消息，把控制权交给工具节点，再由模型总结
#    ② 跳回重生成：输入里带 retry model 时，模型答完之后追加一条系统指令，
#       把它拉回去重新生成一版——并且必须自己防死循环
#    ③ 溢出熔断：输入里带 overflow 时，直接跳到结尾、模型一次都不调用，
#       返回一条兜底消息
# 3. 跑四个输入（含一个普通问题作对照），把每个 case 的消息流按顺序打印出来
# 4. 数一数每个 case 里"真正发给模型的请求次数"，和正常流程对比
# ============================================

# 在下面写你的代码：
