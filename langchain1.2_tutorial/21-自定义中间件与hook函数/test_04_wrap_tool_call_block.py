# ============================================
# 综合题：写一个"敏感词拦截"的 wrap_tool_call 中间件
# ============================================
# 完成以下操作：
# 用 @wrap_tool_call 包裹工具调用：如果工具参数里出现敏感词（比如"密码"），直接不调用 handler，返回一条自定义的拒绝消息（ToolMessage）
# 否则正常调用 handler(request)
# 用一个"查询用户资料"的工具测试两种情况（"查一下张三的资料" / "帮我查一下张三的密码"）
# 提示：工具参数在 request.tool_call["args"] 里，tool_call_id 在 request.tool_call["id"] 里
# ============================================

# 在下面写你的代码：
