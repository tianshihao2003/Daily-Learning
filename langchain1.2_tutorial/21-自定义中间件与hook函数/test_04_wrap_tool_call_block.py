# ============================================
# 题目 2-4：包裹调用——把敏感请求挡在门外，再看洋葱顺序
# ============================================
# 需求：
# 1. 定义一个"查询用户资料"的工具，函数体里打印一行标记
# 2. 用"包裹工具调用"的钩子做敏感词拦截：
#    参数里出现敏感词（比如"密码"）就不执行真工具，直接返回一条拒绝消息；
#    否则正常放行
# 3. 用两种情况各跑一次：查张三的资料 / 查张三的密码
#    确认工具函数体的那行标记只在第一种情况出现
# 4. 再挂两个"包裹模型调用"的钩子，各自在请求前后插一个记号，
#    打印"模型收到的输入"和"最终回答"，说明记号的叠放顺序（谁先动手、谁先返回）
# ============================================

# 在下面写你的代码：
