# ============================================
# 题目 2-2：裁剪——给上下文"按住"上限
# ============================================
# 需求：
# 1. 写一个"模型调用前"的钩子：消息数超过 4 条时，只保留第一条 + 最近几条
#    硬要求：切出来的那一段必须"从用户消息开始"
#    （不能让模型一上来就看见自己说的话——消息列表是人机交替的，切点选错就会出问题）
# 2. 用这个钩子建一个 Agent（带内存检查点），用同一个会话连续跑 5 轮
# 3. 每轮打印状态里的消息条数，确认它被按住、不再无限增长
# 4. 跑通后回答：为什么会切出"以 AI 消息开头"的片段？"取最近几条"到底取几条才刚好？
# ============================================

# 在下面写你的代码：
