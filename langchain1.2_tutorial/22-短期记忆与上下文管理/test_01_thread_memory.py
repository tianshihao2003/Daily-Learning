# ============================================
# 题目1：让 Agent 记住你
# ============================================
# 完成以下操作：
# 创建带 checkpointer=InMemorySaver() 的 Agent
# 用 config={"configurable": {"thread_id": "1"}} 连续 invoke 两轮："你好，我叫小明" → "我叫什么？"
# 确认它能答上来，并打印 len(response["messages"]) 看历史在累积
# 再用 thread_id="2" 问同样的问题，观察它是不是"失忆"了
# ============================================

# 在下面写你的代码：
