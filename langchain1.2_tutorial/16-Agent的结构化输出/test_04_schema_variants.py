# ============================================
# 题目 2-4：同一份 Schema 换三种写法
# ============================================
# 把题目 2-1 的"个人联系信息"再写三遍，每次都从同一段文本里抽取：
# ① 不写类，直接给一个符合 JSON Schema 规范的结构字典
#    （写清：结构名、对象类型、字段说明、哪些字段必填；"手机号"做成可省略的）
# ② 用"带类型的字典"声明（字段写成 类型标注 + 描述，"手机号"可选）
# ③ 用数据类声明（字段说明沿用 2-1 那套写法，记得补 import）
# 每跑完一种就打印结果的类型，然后把三个问题的答案写在注释里：
#    - 拿到的是对象还是普通字典？
#    - 字段说明有没有真的传给模型？（提示：翻一眼发出去的请求体）
#    - 哪种写法会做运行时校验？
# ============================================

# 在下面写你的代码：
