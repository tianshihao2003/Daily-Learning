# ============================================
# 题目 2-3：客户分析报告（查库 → 发邮件 → 出报告）
# ============================================
# 完成以下操作：
# 1. 定义两个工具（docstring 按 Args / Returns 规范写全）：
#    - 查客户数据库：输入查询字符串；命中"张三"返回
#      "客户记录：张三，VIP客户，最近购买日期：2026-01-15，累计消费：$15,000"；
#      命中"李四"返回普通客户记录；其他返回"关于客户xx，无记录"
#    - 发感谢邮件：输入客户名称，返回"已向 xx 发送感谢邮件"
# 2. 定义"客户分析报告"数据模型：
#    - 客户姓名（字符串）
#    - 客户等级：只能是 潜在客户 / 普通客户 / VIP客户 / 流失风险 之一
#    - 最近活动（字符串）
#    - 消费水平：只能是 低 / 中 / 高 之一
#    - 是否已发送感谢邮件（布尔）
#    每个字段都给默认值兜底
# 3. 系统提示词按四步写清顺序：
#    先查库 → 是 VIP 客户才发邮件 → 基于搜索结果生成报告 → 查不到客户就返回空对象、不发邮件
# 4. 用"请分析客户张三"调用，然后打印：
#    - 结构化结果（打印前先判断有没有拿到）
#    - 整条消息链的类型序列（数一数一共几条；最后那条工具消息的 name 是什么）
# ============================================

# 在下面写你的代码：
