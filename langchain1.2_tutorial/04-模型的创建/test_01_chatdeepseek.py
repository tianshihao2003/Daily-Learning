# ============================================
# 题目1：用厂商专用类创建 DeepSeek 模型
# ============================================
# 完成以下操作：
# 1. 加载项目根目录的 .env（让 .env 里的值覆盖已有环境变量）
# 2. 用 DeepSeek 的厂商专用类创建模型（模型名 deepseek-v4-flash）：
#    ① 显式传密钥和地址跑一次 —— 注意这个类的「地址参数名」和 OpenAI 那套不一样
#    ② 不传密钥再跑一次，看它能不能自己从环境变量里读到
#    ③ 换成它接受的另一个等价「模型名参数」再跑一次
# 3. 每次都调用一句「请介绍一下你自己」，打印回复的文本
# ============================================

# 在下面写你的代码：


