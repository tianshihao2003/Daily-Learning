# ============================================
# 综合题：模型自检小工具
# ============================================
# 按顺序完成：
# 1. 从 .env 读配置，用统一接口创建一个 OpenAI 兼容的模型
#    （temperature 取「要一致性、准确性」的那档区间）
# 2. 打印这个实例的完整参数名清单 + 总数
# 3. 按四大分类各挑几个参数名打印（连接类 / 推理类 / 框架通用 / 高级扩展），
#    挑不出来说明名字记错了
# 4. 给它透传「思考开关」（先启用），问一句「1 + 1 = ？」，打印回答
# 5. 打印这次调用的输入 / 输出 / 总 token，
#    并按「中文 1 token ≈ 1–1.8 个汉字」估算输出大约对应多少汉字
# 6. 把开关换成禁用再调一次，对比两次的思考内容与 reasoning token
# ============================================

# 在下面写你的代码：


