# ============================================
# 题目 2-1：用"轻量字典类型"定义结构
# ============================================
# 需求：
# 1. 用 Python 自带的字典类型标注方式（只做类型声明、不做运行时强校验）定义：
#    - 演员结构：姓名、饰演角色（都用中文字符串说明）
#    - 电影结构：片名、上映年份、导演（这三个标成必填）、
#      演员列表（类型是"演员结构的列表"，不标必填）、评分（不标必填）
# 2. 绑定后调用一次，让模型介绍一部电影
# 3. 打印结果和它的类型，确认返回的是不是普通字典
# 4. 用方括号取出片名，以及演员列表里第一个演员的姓名
# 挑战：故意把某个字段的中文说明写得很含糊，看抽取效果有什么变化
# ============================================

# 在下面写你的代码：
