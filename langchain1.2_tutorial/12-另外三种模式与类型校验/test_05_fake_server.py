# ============================================
# 题目 3-1：自己写一个假服务端，亲眼确认"只有 Pydantic 会校验"
# ============================================
# 需求（分三步）：
# 第一步：写一个假服务端
#   - 监听 127.0.0.1:8889，处理 POST 请求
#   - 从请求体里取出工具名并打印出来
#   - 固定返回一份"字段名故意写错"的响应：
#       参数用 {"title1": "盗梦空间", "year2": 2010, "director": "...", "rating": 9.3}
#       （title 和 year 两个字段名都错了）
#   - 并且把结束原因故意写成 "stop"（而不是 "tool_calls"）
#   - 响应里必须包含 choices[0].message.tool_calls[0].function.{name, arguments}，
#     其中 arguments 是字符串形式的 JSON（要 json.dumps）
# 第二步：客户端用 Pydantic 模式调用
#   - 客户端把模型的接口地址指向假服务端（http://localhost:8889），密钥随便填一个假的
#   - 定义电影结构后调用，用 try/except 捕获异常
#   - 数一数有几个 Field required，看看错误信息里的 input_value 是什么
# 第三步：换成"轻量字典类型"那一版再调一次
#   - 观察返回的字典：错误键名 title1 / year2 是不是被原样保留了？
#   - 回答：结束原因被写成 "stop" 有没有影响解析？
# 提示：跑的时候要开两个终端（先启动服务端再跑客户端）；
#       如果写在同一个文件里，记得用 threading 把服务端放到后台线程
# ============================================

# 在下面写你的代码：
