# ============================================
# 题目 2-4：手写 JSON Schema，并显式指定强约束模式
# ============================================
# 需求：
# 1. 不用 Pydantic 类，手写一个字典描述电影结构：
#    片名 / 年份 / 导演 / 评分，并声明哪些字段必填
#    注意：最外层那个"人类可读的标题"键不能写中文（它会被当成工具名）
# 2. 让模型按这份手写结构返回，打印结果和它的类型
# 3. 再加上参数，显式指定走"服务器自己的强约束模式"，再调一次，
#    对比两次的差别（可以用假服务端抓请求，或看报错信息）
# 4. 记一下：DeepSeek 的模型服务不支持这个模式，要挑支持它的平台跑
# ============================================

# 在下面写你的代码：
