# ============================================
# 题目 2-2：参数的说明和类型都从哪来
# ============================================
# 完成以下操作：
# 1. 定义一个工具：参数是城市、带默认值"北京"，docstring 按规范写参数说明；
#    打印工具描述，指出哪个参数多了默认值字段、必填清单里还剩谁
# 2. 复制一份、把类型注解删掉，再看参数模式变成了什么
# 3. 再复制一份，把 docstring 里写的参数改成签名里没有的名字，
#    看会发生什么（是定义时就炸，还是调用时才炸？）
# 4. 用装饰器自带的描述字段覆盖 docstring，确认最终描述用的是哪一个
# 5. 打开解析开关，让 docstring 里的参数说明真正填进各个参数里
# ============================================

# 在下面写你的代码：


