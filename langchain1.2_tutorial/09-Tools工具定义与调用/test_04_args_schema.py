# ============================================
# 题目 2-4：用 Pydantic / JSON Schema 定义参数结构
# ============================================
# 完成以下操作：
# 1. 用 Pydantic 定义一个参数模型：
#    城市带描述与默认值、单位只能在"摄氏度 / 华氏度"两个取值里选、
#    是否要预报给个布尔默认值
# 2. 把它接到工具上，打印工具描述，
#    指出哪个参数多了枚举数组、还有没有必填清单
# 3. 故意传一个不在取值范围内的单位，看在本地还是服务端被拦下、报什么错；
#    再试试用位置参数初始化这个模型
# 4. 换一种做法：直接用一份 JSON Schema 字典当参数结构（只写参数模式那一段），
#    打印工具描述，验证它是不是原样输出
# 5. 故意让字典里的键和函数参数名对不上，调用一次看报什么错
# ============================================

# 在下面写你的代码：


