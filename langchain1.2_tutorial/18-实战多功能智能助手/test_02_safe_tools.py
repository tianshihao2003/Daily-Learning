# ============================================
# 题目 2-2：写两个"安全"的工具
# ============================================
# ① 数学计算工具：
#    - 支持 + - * / ** 和 sqrt / pow / abs / round / sin / cos / tan / log、常量 pi / e
#    - 执行表达式时要"禁用 Python 内置函数"，只放开白名单里的数学函数
#    - 出错时返回"计算出错 + 支持哪些函数"
# ② 时间信息工具：
#    - 用一个"枚举式参数"顶五个功能：当前时间 / 今天日期 / 明天 / 昨天 / 星期几
#    - 参数带默认值；传不认识的值时返回支持列表
# 两个工具都不经过模型，直接单独调用验证：
#    - 算 (25 + 17) * 3
#    - 算 sqrt(16) + abs(-5)
#    - 故意算一个危险表达式（比如 __import__('os').system('echo hi')）
#    - 不带参数调用时间工具
#    - 传一个不支持的时间类型
# ============================================

# 在下面写你的代码：
