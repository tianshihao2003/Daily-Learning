# ============================================
# 题目 2-3：把助手封装成一个类
# ============================================
# 把"模型 + 工具 + 系统提示词 + 对话历史"打包成一个类，对外只暴露两个方法：
# ① 初始化：
#    - 工具列表：天气 / 计算 / 时间 / 货币 / 搜索
#    - 系统提示词：先判断该用哪个工具 → 需要多个就按顺序调用 → 用友好专业的语气、
#      通俗易懂的语言解释结果 → 无法完成时如实说明（始终用中文回答）
#    - 创建一个 Agent，并准备一个空的对话历史
# ② chat(用户输入)：
#    - 先把用户输入追加进历史
#    - 带着"完整历史"调用 Agent
#    - 用返回的完整消息列表覆盖历史
#    - 从后往前找"最后一条有内容的 AI 消息"返回；都找不到就返回一句兜底话
# ③ reset()：清空对话历史
# 写完后"不调模型"也要能自测：
#    - 实例化这个类，打印 chat / reset 两个方法的签名
#    - 往历史里塞几条假消息，调用 reset() 看是否清空
# ============================================

# 在下面写你的代码：
