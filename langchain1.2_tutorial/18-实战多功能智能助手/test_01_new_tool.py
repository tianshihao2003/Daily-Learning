# ============================================
# 题目 2-1：给助手加第 6 个工具
# ============================================
# 完成以下操作：
# 1. 写一个"单位换算"工具（主题可以自己选）：
#    - 参数至少三个：数值 + 源单位 + 目标单位，其中单位参数带默认值
#    - 遇到不认识的值时返回"支持哪些单位"，而不是报错
#    - docstring 按 Args / Returns / Examples 规范写全
# 2. 先单独调用一次这个工具（不经过模型），确认换算结果正确
# 3. 把它加进助手的工具列表，问一个"只有新工具能回答"的问题，
#    并打印消息链里的工具调用名，确认模型选的就是它
# ============================================

# 在下面写你的代码：
