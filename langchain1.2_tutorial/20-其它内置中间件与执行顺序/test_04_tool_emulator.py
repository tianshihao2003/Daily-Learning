# ============================================
# 题目 2-4：工具还没开发完？让 LLM 假装自己就是那个工具
# ============================================
# 需求：
# 1. 定义一个"查天气"的真工具，函数体里打印一行标记
# 2. 建一个 Agent，让另一个模型"假装自己就是那个工具"，
#    直接编一份像样的天气 JSON 返回给主模型（记得显式指定用哪个模型来编）
# 3. 问"今天北京天气如何"，打印返回的每一条消息（类型 + 内容）
# 4. 检查三件事：
#    ① 真工具的函数体有没有执行（看那行标记）
#    ② 一次调用里一共发生了几次模型请求
#    ③ 用一句话说清这个中间件的定位（临时替代品？能上生产？）
# ============================================

# 在下面写你的代码：
