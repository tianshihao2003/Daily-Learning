# ============================================
# 题目2：让工具失败后自动重试
# ============================================
# 完成以下操作：
# 写一个"前两次必失败、第三次成功"的工具（全局计数器 + raise TimeoutError）
# 配上 ToolRetryMiddleware(max_retries=3, initial_delay=0.5, backoff_factor=2.0, jitter=True, retry_on=(TimeoutError,))
# 在工具里打印时间戳，观察"等待越来越久 + 有抖动"的效果
# 确认最终能拿到结果
# ============================================

# 在下面写你的代码：
