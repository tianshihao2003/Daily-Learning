# ============================================
# 题目 2-3：让工具失败后自动重试
# ============================================
# 需求：
# 1. 写一个"前两次必失败、第三次成功"的工具：全局计数器 + 抛超时异常
#    每次调用都打印时间戳
# 2. 给它配上自动重试：最多重试 3 次 / 第一次等 0.5 秒 / 之后每次等待翻倍 /
#    等待上限 5 秒 / 带随机抖动 / 只对超时异常重试 / 重试仍失败就把错误交回模型
# 3. 跑一次，打印最终回答、工具总共被调用的次数、总耗时
#    从时间戳确认等待间隔越来越久，并确认最终拿到了结果
# 挑战：把"翻倍因子"改成 0、并关掉抖动，看看两次等待变成什么样
# ============================================

# 在下面写你的代码：
