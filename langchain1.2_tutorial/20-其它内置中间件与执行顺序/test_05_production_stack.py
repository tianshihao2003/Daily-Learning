# ============================================
# 题目 3-1（综合）：组合出一套"生产级"中间件
# ============================================
# 需求：
# 1. 按"先检查拦截、再增强兜底"的顺序把四条中间件串成一条链：
#    敏感信息脱敏 → 模型调用次数限制 → 上下文编辑 → 工具重试
#    每条旁边用注释写清"为什么排在这个位置"
# 2. 用一个"偶尔超时"的工具（每三次成功一次）验证兜底重试真的在工作
# 3. 跑一次带敏感信息的多步任务（比如"我的邮箱是 a@b.com，用它查一下北京天气"），
#    确认模型收到的输入里敏感信息已经被替换掉，并且最终拿到了回答
# 4. 把上下文编辑的触发阈值调小（比如 50），看工具结果在"发给模型的那一份"里
#    被换成了什么；再用一句话说明它和"摘要"的区别（改状态 vs 改副本）
# 5. 亲手验证顺序与"重名"：写两个只打印自己名字的中间件（模型调用前后各打印一行），
#    分别用两种书写顺序各跑一次，确认"调用前正序、调用后逆序"；
#    再故意把同一个类实例化两次挂上去，记录报错原文并说出原因
# ============================================

# 在下面写你的代码：
