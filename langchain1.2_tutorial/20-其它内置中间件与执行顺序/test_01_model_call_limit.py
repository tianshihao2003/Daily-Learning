# ============================================
# 题目 2-1：给 Agent 戴上"紧箍咒"
# ============================================
# 需求：
# 1. 建两个 Agent 做对比，都给同一个"会停不下来"的任务
#    （比如让模型反复调用同一个加法工具）
# 2. 第一个 Agent 限制"模型调用次数"：每次运行最多 2 次
#    - 先让它优雅收尾：打印返回消息的条数与最后一条的类型
#    - 再把它改成"直接抛错"，用 try/except 记录异常类型与信息
# 3. 第二个 Agent 限制"工具调用次数"：只盯住加法工具、每次运行最多 2 次
#    - 先保持默认行为：打印那条"超限"的工具结果长什么样
#    - 再把它改成"直接抛错"，记录异常类型与信息
# ============================================

# 在下面写你的代码：
