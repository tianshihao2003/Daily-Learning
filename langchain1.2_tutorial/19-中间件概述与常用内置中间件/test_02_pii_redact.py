# ============================================
# 题目2：用 PII 中间件拦住邮箱
# ============================================
# 完成以下操作：
# 用 PIIMiddleware("email", strategy="redact", apply_to_input=True) 创建 Agent
# 发一句带邮箱的话，打印返回的消息列表，确认邮箱已被替换成 [REDACTED_EMAIL]
# 挑战：把 strategy 换成 "hash" 和 "block"，观察两者的区别（block 会直接抛异常）
# ============================================

# 在下面写你的代码：
