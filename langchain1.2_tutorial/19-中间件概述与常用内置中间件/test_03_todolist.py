# ============================================
# 题目3：给待办清单中间件搭个世界观
# ============================================
# 完成以下操作：
# 用 TodoListMiddleware() 创建一个"能修复代码"的 Agent（工具：读文件、写文件、跑测试，可用简单的模拟实现）
# 问一个多步任务（比如"读取 my_add.py，检查加法对不对，不对就修，再跑一次测试"）
# 打印消息列表，找出 write_todos 的调用和它的工具返回；观察 todos 里的 content / status 结构
# 提示：任务要"多步且前后依赖"，否则模型不会去写清单
# ============================================

# 在下面写你的代码：
