# ============================================
# 题目 3-1（综合）：让 Agent 自己修好"测试跑不过"的代码
# ============================================
# 需求（分四步）：
# 1. 在工作区目录里准备两个文件（工作区放在脚本同级目录下，路径基于脚本自身位置计算）：
#    - my_add.py：故意把"和"写成"差"
#    - test_my_add.py：四条断言（专门抓这个 Bug）
#    先在终端里手动跑一次 pytest，记住"红"的样子
# 2. 写四个工具：列目录 / 读文件 / 写文件 / 跑测试
#    跑测试内部调 pytest，输出里带上 returncode
# 3. 建 Agent：挂上"待办清单"中间件，四个工具全给它；
#    系统提示词里要求"先列清单再动手"
# 4. 让它执行：请测试并修复工作区下 my_add.py 文件中的代码；然后
#    ① 从消息里找出它写清单的那次工具调用与工具返回
#    ② 打印这次运行最终状态里那份独立于消息列表的清单
#    ③ 看一眼文件内容是不是真的修好了
# ============================================

# 在下面写你的代码：
