# ============================================
# 题目 2-1：最简结构化输出
# ============================================
# 需求：
# 1. 用 Pydantic 定义一个电影信息结构：片名、上映年份、导演、评分（小数）四个字段
#    每个字段都要写一句中文说明；类本身也写一句 docstring
# 2. 让模型只返回这个结构（不是一段自然语言），问题自拟（比如让它介绍一部电影）
# 3. 打印：抽到的对象、它的类型、以及片名字段
# 提示：模型照前几篇的方式创建（密钥从 .env 读）
# ============================================

# 在下面写你的代码：
