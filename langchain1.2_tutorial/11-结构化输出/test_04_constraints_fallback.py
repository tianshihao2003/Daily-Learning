# ============================================
# 题目 2-4：严格约束 + 兜底校验
# ============================================
# 需求：给商品结构加上严格约束：
#   - 商品名称：普通字段
#   - 价格：数值，必须大于 0
#   - 库存：整数，不能为负
# 然后故意问一个"违法"的问题，比如：
#   "华为mate 80 promax 价格是 -7999，当前库存 -100"
# 做三件事：
# 1. 用 try/except 接住校验失败的异常，打印出具体的错误信息（哪个字段、什么错）
# 2. 在成功拿到数据的分支里，用代码把不合理的关键字段兜底成 7999 / 0
# 3. 观察这次校验失败**只发了一次请求**（说明框架不会自动重试），
#    想亲眼看到异常，可以照第 12 篇综合题的做法用假服务端硬返回负数
# ============================================

# 在下面写你的代码：
