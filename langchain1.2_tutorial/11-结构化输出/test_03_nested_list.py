# ============================================
# 题目3：嵌套 + 列表抽取
# ============================================
# 完成以下操作：
# 1. 定义 Actor 模型：name(演员姓名)、role(饰演角色)
# 2. 定义 MovieDetail 模型：title(电影名)、cast(List[Actor]，演员列表)、rating(评分)
# 3. 绑定 with_structured_output 后调用，让模型介绍一部电影的主演和评分
# 4. 遍历结果里的 cast，打印每个演员的姓名和角色
# 5. 挑战：把 MovieDetail 再套一层（比如加一个 director 字段，它的类型是另一个模型），
#    观察嵌套层数增加后抽取效果有没有变差
# ============================================

# 在下面写你的代码：
