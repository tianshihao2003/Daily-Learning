# ============================================
# 综合题：给一次调用装上「仪表盘」
# ============================================
# 写一个函数，实现「一次带仪表盘的调用」：
# 1. 创建模型（从 .env 读配置）
# 2. 调用时带上：运行名、标签、业务元数据（用户 ID / 会话 ID）
# 3. 用 try/except 包住调用：
#    - 分别处理「配置错误」「网络错误」
#    - 最后留一个兜底分支
# 4. 调用成功后打印这些读数：
#    运行名 / 标签、模型名、提供商、结束原因、
#    输入与输出 token、首字耗时（该平台不返回就说明原因）、运行 ID
# 5. 用整洁的方式打印回复正文
# 6. 最后把地址换成一个连不上的端口再跑一次，验证异常分支
#    （想一想：为什么「网络错误」那一支没被命中？）
# ============================================

# 在下面写你的代码：


