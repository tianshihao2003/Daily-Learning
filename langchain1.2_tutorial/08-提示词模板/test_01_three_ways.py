# ============================================
# 题目 2-1：一个提示词，三种"取结果"方式
# ============================================
# 完成以下操作：
# 1. 用字符串拼接写一句"你是一个 {难度} 级别的编程导师，请解释 {主题}"，直接调用模型
# 2. 用字符串模板重写同一句：用它的填充方法拿到最终提示词，再调用模型
# 3. 用聊天模板把同一句拆成"系统消息 + 用户消息"两条，
#    然后分别用三种调用方式把变量填进去，打印每次返回值的【类型】
# 4. 把其中"能直接交给模型"的那一份传给模型，打印回复
# 提示：三种方式的参数写法不一样（一个收字典、两个收关键字参数）
# ============================================

# 在下面写你的代码：


